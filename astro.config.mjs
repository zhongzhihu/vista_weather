import { defineConfig } from "astro/config";

// https://astro.build/config
export default defineConfig({
  site: "https://zhongzhihu.github.io",
  base: "/vistaweather_website",
  compressHTML: true,
});
